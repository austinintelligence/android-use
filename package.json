{
  "name": "android-use-workspace",
  "private": true,
  "workspaces": ["install"],
  "scripts": { "test": "npm test --workspace install", "lint": "npm run lint --workspace install", "verify": "cargo xtask verify" },
  "engines": { "node": ">=20.11" }
}
