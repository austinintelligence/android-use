interface:
  display_name: "Android Use"
  short_description: "Fast, bounded Android control through au"
  default_prompt: "Use $android-use for authorized Android control. Prefer one daemon-backed batch or tape, -w wire proof, semantic handles, and text-first deltas; reuse the validated device session, confirm privacy-sensitive or destructive work, and clean up AU-owned resources."
